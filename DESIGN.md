# 災害板 Design System

この文書は、災害板（saigaiban）の現在の画面設計を記録するデザイン仕様書です。新しい画面を追加するときは、見た目だけでなく「公式情報ではないものを公式と誤認させない」「次に確認すべき場所へ迷わず進める」という役割を守ります。

## 1. デザインの目的

- 被災時でも読みやすく、落ち着いて次の行動へ進める
- OpenNaviの公式情報と、災害板の場所カード・現地報告を明確に分ける
- 場所カードを「営業中」「開設中」「安全」と誤認させない
- 情報がない状態も隠さず、公式ハブへの導線を残す
- 低速回線、スマートフォン、文字拡大、キーボード操作を前提にする

ビジュアルの方向性は「明るい空気感のある信頼性」です。白いカード、淡い空色の背景、ティールの導線色、ミント・黄色・コーラル・ラベンダーの補助色で、緊張を煽らずに情報の違いを伝えます。

## 2. 色のトークン

現在のCSSは `src/html.ts` の `CSS` 定数が正本です。

| 役割 | 値 | 用途 |
|---|---|---|
| 背景 | `#eef7fb` | ページ全体 |
| 本文 | `#173247` | 本文・見出し |
| 補助文 | `#5c7180` | 時刻、注記、説明 |
| カード | `#ffffff` | 場所カード、パネル |
| 境界線 | `#c8dce8` | 枠線、区切り |
| 主導線 | `#0b6b78` | ボタン、選択中タブ、主要リンク |
| 主導線の濃色 | `#07525f` | hover、強調状態 |
| ミント | `#dff4ed` | 店側自己申告、成功、確認済み補助 |
| スカイ | `#deeffc` | hover、選択補助、表ヘッダー |
| サン | `#fff0c2` | 未確認、注意、状態タグ |
| コーラル | `#ffe1d9` | カードの周期アクセント |
| ラベンダー | `#e8e7fb` | カードの周期アクセント |

色だけで意味を表さないこと。状態は必ず短いテキスト、見出し、またはアイコン以外のラベルと組み合わせます。

## 3. タイポグラフィ

- フォント: `Hiragino Sans`, `Noto Sans JP`, `system-ui`, sans-serif
- 本文の行間: `1.7`
- リード文: 本文より少し大きく、最大幅 `68ch`
- ページ見出し: 大きく、短く、最大幅 `20ch`
- 補助文: 本文より小さくするが、情報を削らない
- 日本語の長文は1段落を短くし、見出しの下に1つの役割だけを書く

見出しの後ろに伸びる細い線と、ページ見出し下の3色バーを使い、階層を色だけに依存せずに示します。

## 4. レイアウト

- ページ最大幅: `1120px`
- モバイル最小幅: `320px`
- 標準の外側余白: `20px`
- モバイルの外側余白: `14px`
- カード一覧: 画面幅に応じたauto-fitグリッド
- 標準カード幅: 最小 `260px`
- 720px未満: カードを1列にする
- タップ対象: 最小高さおよそ `40px` を目安にする

画面の順番は、原則として次の通りです。

1. 公式ではないことのバナー
2. 災害板、公式ハブ、補助ページへのナビゲーション
3. ページ見出しと短い説明
4. 地域選択または困りごとの絞り込み
5. 場所カード、報告、次の行動
6. 公式情報・出典・注意書き

## 5. 共通コンポーネント

### 公式ではないことのバナー

ページ上部に濃いティールの帯を置き、災害板が公式窓口ではないことを固定表示します。営業、避難所開設、安全の断定はこのバナーと本文の両方で避けます。

### ナビゲーション

半透明の白いパネルと角丸のリンクを使います。最初のリンクだけ主導線色で強調します。ナビゲーションは折り返し可能にし、スマートフォンでも横に溢れさせません。

### 場所カード

カードには次の順で置きます。

1. 状態タグ（未確認、投稿、店側の自己申告など）
2. 場所名
3. 住所や避難所台帳の注記
4. 報告の立場、時刻、鮮度
5. 地図と詳細ページへの導線

カード上部の短いアクセントバーは、4種類の色を周期的に使います。色の違いは装飾であり、状態や信頼度を意味しません。

### 絞り込みと検索

市区町村ページの絞り込みは丸いカテゴリリンクで表示し、選択中は主導線色にします。検索入力とボタンは隣接させ、検索結果件数と現在の条件を入力欄の下に表示します。

### 公式情報パネル

公式リンク、支援情報、引き継ぎ仕様などは、白から淡い青へ変化するパネルにまとめます。災害板の報告と公式情報を同じ状態タグで表示しません。

### 状態タグ

状態タグは短く、丸い小さなラベルにします。`未確認`、`投稿`、`店側` など、誰の情報かが分かる言葉を優先します。`営業中`、`開設中`、`安全` は災害板の判断として使いません。

## 6. 動きと操作感

- ページ見出し、リード、検索パネル、カードは短い上方向のsoft-riseを使う
- hoverではカードを少し持ち上げ、枠線と影を強める
- ボタンのactiveでは少し沈ませる
- `prefers-reduced-motion: reduce` の利用者にはアニメーションを出さない
- focus時は3px程度のティール系アウトラインを表示する

動きは情報を発見しやすくするためだけに使い、緊急性や危険度を煽る点滅、連続スクロール、常時更新は使いません。

## 7. コンテンツと情報境界

- 場所マスターの正体と、現地報告の観測を別の情報として扱う
- 公式情報へのリンクを、現地報告より先に置く
- 報告は見た時点の補足であり、公式確認ではないと明記する
- 住所、電話、氏名、待ち合わせ、個人間の仲介を表示しない
- 地図リンクは検索リンクを使い、台帳の座標URLをそのまま表示しない
- OpenNaviの公式ハブと災害板を「使い分け」として案内する
- 現地サイトが立ち上がった後は、現地サイトを正本として案内する

## 8. 仕様書・プロトコルへの導線

現地サイト運営者が受け渡し手順を確認できるよう、次の導線を維持します。

- アバウトページ: `/about` の「現地サイトへの引き継ぎ」
- 公開仕様書: `/protocol/opennavi/v1`
- 機械向け発見情報: `/.well-known/opennavi.json`
- 正規データAPI: `/api/opennavi/v1/handoff/{area-slug}`

## 9. 変更時のチェック

画面や色を変更したら、最低限次を確認します。

- 公式情報と住民報告の区別が残っている
- 未確認状態が色だけでなくテキストでも読める
- 320px幅と文字拡大で横スクロールしない
- focus表示とキーボード移動が残っている
- reduced-motionで動きが止まる
- `/about`、`/protocol/opennavi/v1`、主要市区町村ページのリンクが切れていない
- localhost、内部API、個人情報が公開HTMLに混ざっていない

## 10. ソースオブトゥルース

デザインの実装は `/Users/kokubee/code2026/saigaiban/src/html.ts`、プロトコルの契約は `/Users/kokubee/code2026/saigaiban/src/protocol.ts` と `/Users/kokubee/code2026/saigaiban/src/handoff.ts` が正本です。この文書は画面の意図と再利用ルールを説明するもので、CSSの値を自動生成するファイルではありません。
